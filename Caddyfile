# Основной сайт (калькуляторы)
{$DOMAIN} {
    reverse_proxy app:8501
}

{$APEX_DOMAIN} {
    redir https://{$DOMAIN}{uri} permanent
}

# Риск-рейтинг (внутреннее тестирование)
{$RISK_DOMAIN} {
    reverse_proxy risk_app:8502
}
