[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "risk-module"
version = "0.1.0"
description = "Modular risk toolkit"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["risk_module*"]

